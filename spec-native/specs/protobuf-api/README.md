# protobuf-api

Iniciativa para exponer el mismo núcleo durable de Ixmati mediante gRPC y
REST con `application/protobuf`, manteniendo REST/JSON.

La fuente de verdad del contrato es [`proto/`](../../../proto/). El estado de
implementación y las pruebas se mantienen en
[`TASKS.md`](../../tasks/protobuf-api/TASKS.md).
