# mosquitto.conf — broker ephemeral para quickstart local
# Sin persistencia: los datos se pierden al detener el container.

listener 1883
protocol mqtt

persistence false
allow_anonymous true

log_dest stdout
log_type error warning notice
