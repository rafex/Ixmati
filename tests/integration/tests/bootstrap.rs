#[cfg(test)]
mod tests {
    #[test]
    fn bootstrap_integration_placeholder() {
        // TODO: reemplazar con tests de integracion reales
        // que crucen fronteras de crates (writer+core+cache, SQLite :memory:, Mosquitto real)
        assert!(true, "placeholder para tests de integracion");
    }
}
